<h1>Who does what?</h1>
<p>Whatsits are completed by Whosits. Each Whosit does zero or more Whatsits, and each Whatsit is done by zero or more Whosits.</p>
<form id="addHasA" action="/hasA" method="post">
	<fieldset>
		<legend>Enter a new Whatsit for a Whosit</legend>
		<label for="whosit">Whosit: </label>
		<select id="whosit" name="whosit_id" required>
            <option value="">Choose a Whosit</option>
			{{#each whosit}}
            <option value="{{whosit_id}}">{{name}}</option>
            {{/each}}
		</select><br>
		<label for="whatsit">Whatsit: </label>
		<select id="whatsit" name="whatsit_id" required>
            <option value="">Choose a Whatsit</option>
			{{#each whatsit}}
            <option value="{{whatsit_id}}">{{name}}</option>
            {{/each}}
		</select><br><br>
		<input type="submit" name="hasASubmit" value="Submit">
	</fieldset>
</form>
<h3>Whatsits Assigned to Whosits</h3>
<table>
  <thead> 
      <th>Name of Whosit</th>
      <th>Name of Whatsit</th>
  </thead>
  <tbody>
      {{#each entry}} 
      <tr> 
          <td>{{whosit}}</td> 
          <td>{{whatsit}}</td>
          <td><button onclick="deleteHasA({{whosit_id}},{{whatsit_id}})">Delete</button></td>
      </tr> 
      {{/each}} 
  </tbody>
</table>
